name: Deploy to NCP VM

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Inject Secrets into application.yml
        run: |
          echo "spring.datasource.url=${{ secrets.DB_URL }}" > src/main/resources/application.yml
          echo "spring.datasource.username=${{ secrets.DB_USER }}" >> src/main/resources/application.yml
          echo "spring.datasource.password=${{ secrets.DB_PASSWORD }}" >> src/main/resources/application.yml

      - name: Build with Gradle
        run: ./gradlew clean build

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            cd ~/app
            git pull origin main
            ./gradlew bootJar
            pkill -f 'java -jar' || true
            nohup java -jar build/libs/*.jar > app.log 2>&1 &